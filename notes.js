/**
 *  @function describe() é utilizado para agrupar testes, podendo catalogar e abstrair de maneira mais específica.
 *  @function it() é utilizado testar uma função importada para seu teste, passando o @function expect() @function toBe().
 */

/**
 *  Para rodar códigos antes e depois dos testes unitários:

 *  @function beforeAll() utilizada para rodar códigos antes de todos os testes.
 *  @function beforeEach() utilizada para rodar códigos antes de cada teste, um a um.
 *  @function afterAll() utilizada para rodar códigos após todos os testes.
 *  @function afterEach() utilizada para rodar códigos após de cada teste, um a um.
 */